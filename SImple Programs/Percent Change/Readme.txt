Simple script created to save some time on a repetitive workplace task.

Uses a pandas dataframe to read an excel file and detremine the % increase/decrease and adds this into a new column, then saves the dataframe as a new spreadsheet
